# Pinned, tested versions for offline installer bundles.
telethon==1.42.0
deltachat-rpc-client==2.47.0
deltachat-rpc-server==2.47.0
tomli==2.3.0; python_version < "3.11"
# PySocks enables SOCKS5/HTTP proxy support (proxy.type = "socks5" or "http").
# Required when using Karing, Shadowsocks clients, or any local SOCKS5 proxy.
# Not bundled as a vendor wheel; the installer falls back to online PyPI if needed.
PySocks>=1.7.1
